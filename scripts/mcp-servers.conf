# MCP Servers Configuration
# Format: name|directory|port|endpoint|type
# 
# name: A unique identifier for the MCP server
# directory: Relative path from the mcp/ base directory
# port: The localhost port where the server will be accessible
# endpoint: The MCP endpoint path (optional, defaults to /mcp)
# type: Connection type - http or sse (optional, defaults to http)
#
# Example:
# myserver|myserver|3034|/mcp|http

# Mermaid diagram generator MCP server
mermaid|mermaid|3033|/mcp|http
